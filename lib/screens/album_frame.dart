import 'package:flutter/material.dart';

class AlbumFrame extends StatelessWidget {
  const AlbumFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        leading: Icon(Icons.arrow_back_ios_new),
      ),
    );
  }
}
